<form method="post" action="{{ path('admin_customer_delete', {'id': customer.id}) }}" onsubmit="return confirm('Voulez-vous réelement supprimer cet element ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ customer.id) }}">
    <button class="btn btn-danger text-white mt-2"><i class="fa fa-times"></i></button>
</form>

<form method="post" action="{{ path('admin_declination_delete', {'id': vehicle_declination.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vehicle_declination.id) }}">
    <button class="btn">Delete</button>
</form>

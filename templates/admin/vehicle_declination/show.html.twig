{% extends 'base.html.twig' %}

{% block title %}VehicleDeclination{% endblock %}

{% block body %}
    <h1>VehicleDeclination</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ vehicle_declination.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ vehicle_declination.name }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_declination_index') }}">back to list</a>

    <a href="{{ path('admin_declination_edit', {'id': vehicle_declination.id}) }}">edit</a>

    {{ include('admin/vehicle_declination/_delete_form.html.twig') }}
{% endblock %}
